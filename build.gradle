buildscript {
    repositories {
        jcenter()
        maven { url = "http://files.minecraftforge.net/maven" }
    }
    dependencies {
        classpath 'net.minecraftforge.gradle:ForgeGradle:2.3-SNAPSHOT'
    }
}
apply plugin: 'net.minecraftforge.gradle.forge'

version = "${minecraft_version}-${mod_version}"
group = "epicsquid.superiorshields"
archivesBaseName = "SuperiorShields"

sourceCompatibility = targetCompatibility = '1.8' // Need this here so eclipse task generates correctly.
compileJava {
    sourceCompatibility = targetCompatibility = '1.8'
}

minecraft {
    version = "1.12.2-14.23.5.2768"
    runDir = "run"

    mappings = "snapshot_20171003"

    replace "@VERSION@", project.version
    replaceIn "SuperiorShields.java"
}

dependencies {
    if (!project.hasProperty("dev_env")) {
        compile files('libs/')
    } else {
        compile project(':MysticalLib')
    }

    deobfCompile "com.azanor:Baubles:1.12-${baubles_version}"
    compile "de.ellpeck.naturesaura:NaturesAura:${natures_aura_version}"
    compile "vazkii.botania:Botania:${botania_version}"
}

repositories {
    maven {
        name 'McModDev Maven'
        url = "https://maven.mcmoddev.com/"
    }
    maven {
        name = 'Natures Aura'
        url = "https://maven.chaosfield.at"
    }
    maven {
        name = 'Botania'
        url = "https://maven.blamejared.com/"
    }
}

processResources {
    // this will ensure that this task is redone when the versions change.
    inputs.property "version", project.version
    inputs.property "mcversion", project.minecraft.version

    // replace stuff in mcmod.info, nothing else
    from(sourceSets.main.resources.srcDirs) {
        include 'mcmod.info'
                
        // replace version and mcversion
        expand 'version':project.version, 'mcversion':project.minecraft.version
    }
        
    // copy everything else except the mcmod.info
    from(sourceSets.main.resources.srcDirs) {
        exclude 'mcmod.info'
    }
}
